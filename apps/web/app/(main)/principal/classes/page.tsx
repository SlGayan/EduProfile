// Shared with /admin/classes — both roles use the same Class Management
// implementation (see middleware.ts SHARED_ROUTES and the role guard inside
// ClassManagementPage).
export { default } from "@/components/class-management-page"
