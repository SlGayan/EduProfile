{
    "name": "eduprofile-root",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "npm run build -ws --if-present",
        "dev": "npm run dev -ws --if-present",
        "test": "npm run test -ws --if-present"
    }
}